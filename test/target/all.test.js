#!/usr/bin/env node
var suman = require('suman');
var Test = suman.init(module);
Test.create(function (assert, sumanUtils, it) {
    {
        checkForEquality,
            onceAsync;
    }
    sumanUtils;
    it(checkForEquality.name, function (t) {
    });
});
